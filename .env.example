# Copy this file to .env and set the backend URL before running npm run build
# For local Docker:  leave empty or set to http://localhost:8080
# For a server:      set to http://<your-server-ip>:8080 or https://api.yourdomain.com
VITE_API_URL=http://localhost:8080
