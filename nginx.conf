server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Every request that doesn't match a static file
    # falls back to index.html — this is required for React Router
    # Without this: refreshing /dashboard returns 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}
